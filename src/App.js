import * as React from 'react';
import { Provider } from 'react-redux';
import Header from './components/Header';

export default () => (
    <React.Fragment>
        <Header />
    </React.Fragment>
);
